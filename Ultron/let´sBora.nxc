#include "calibracao.h"

task main ()
{
   SetSensorColorBlue(S1);
   SetSensorColorBlue(S4);
   
   while (true)
   {
       menuEscolha (); 
       if (ButtonPressed(BTN2, false))
       {
         //  iniciar (); 
       }
       else if (ButtonPressed(BTN2, false))
       {
           calibrar ();  
           // algum comando para salvar a calibração assim  que for feita
       }       
   }
}

